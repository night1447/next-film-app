const Endpoints = {
    AUTH: {
        LOGIN: 'localhost:5000/auth/login',
        REFRESH: 'localhost:5000/refresh',
        LOGOUT: 'localhost:5000/logout',
        PROFILE: 'localhost:5000/profile',
    }
}

export default Endpoints